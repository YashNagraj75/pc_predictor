# Environments -- regenerate with uv (see Q1-PhaseA/README or provision.sh)
.venv/
__pycache__/
*.pyc

# Datasets -- downloadable, too large for git (GitHub hard-blocks >100MB anyway)
data/

# Notebook checkpoints, OS junk
.ipynb_checkpoints/
.DS_Store

# Scratch and duplicated outputs -- canonical probe outputs live in
# Q1-PhaseA/runs/<run>/; these top-level copies were byte-identical strays.
_*.py
*.pre_fix_bak
*.prepatch
*.prefigpatch
/probes_*.json
/recon_*.png
/tsne_*.png
